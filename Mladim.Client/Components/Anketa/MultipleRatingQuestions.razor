@using Mladim.Client.ViewModels.Survey;



<MudText Typo="Typo.h2">@($"{this.Number}. {this.Question.Texts.FirstOrDefault()}")</MudText>

 <MudStack Spacing="3">

    @foreach (var item in Question.Texts.Take(1..))
    {
        
    }

</MudStack>






@code
{
    [Parameter]
    public int Number { get; set; }

    [Parameter]
    public SurveyQuestionVM Question { get; set; }

    [Parameter]
    public SurveryMultipleResponseVM SurveryResponse { get; set; }


    private void ValueChanged(double value)
    {
       
    }

}