@using Mladim.Client.ViewModels.Anketa;


<MudText Typo="Typo.h2">@($"{this.Number}. {this.Question.Text}")</MudText>

    <MudStack Spacing="3" Class="mx-4" Justify="Justify.FlexStart" AlignItems="AlignItems.Start">

    @foreach (var item in Question.RatingResponseQuestions)
    {
        
    }

</MudStack>






@code
{
    [Parameter]
    public MultipleRatingQuestionsVM Question { get; set; }

    [Parameter]
    public int Number { get; set; }


    protected override Task OnInitializedAsync()
    {
        return base.OnInitializedAsync();
    }

}